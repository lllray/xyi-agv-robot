#ifndef __struct_map_config_H
#define __struct_map_config_H

    // priority: min > max
    enum Nnode{ Nnode_Unkown=0 };       // 0% - 100%: p
    enum Mnode{ Mnode_Unkown=-1, Mnode_Path=0, Mnode_Obs=100};
    enum Disnode{ Disnode_Unkown=255};

#endif
